#--------------========== My Linux configuration ===========----------------

# Base 
sudo apt install python3-pip git ssh mosh htop libreoffice gnome-tweaks ubuntu-restricted-extras p7zip-rar rar unrar unace arj cabextract translate-shell flameshot obs-studio peek cmus neofetch

# Multimedia Codecs
sudo apt install ubuntu-restricted-extras

#   --- Vim/Tmux ---

# Tmux
sudo apt install tmux

# gpakosz
https://github.com/gpakosz/.tmux

# add tmux conf 
cp ~/Documents/MyConfig/linux/conf_files/.tmux.conf.conf_files ~/

# Vim
sudo apt install vim

# add vimrc
cp ~/Documents/MyConfig/linux/conf_files/.vimrc ~/

# swaps and backups
mkdir ~/.vim/sessions

# add Vumdle ":PluginInstall"
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

# majutsushi/tagbar
sudo apt install exuberant-ctags

#   --- aliases ---
# add bash_aliases
cp ~/Documents/MyConfig/linux/conf_files/.bash_aliases ~/

#   --- env ---
# docker/compose
Install Docker: https://docs.docker.com/install/linux/docker-ce/ubuntu/
Install Docker Compose: https://docs.docker.com/compose/install/

#   --- apps ---
# snap
sudo snap install gimp
sudo snap install telegram-desktop
sudo snap install zoom-client
sudo snap install postman
sudo snap install discord --classic

# remove  os auto_upgrade
sudo apt autoremove unattended-upgrades

#   --- Gnome Interfase ---

# Пароль звездочками: *******
# In bash
sudo visudo

# Add under Defaults env_reset
Defaults pwfeedback

# save
«Ctr+X»
«Y»

# Включить сворачивание при клике
$ gsettings set org.gnome.shell.extensions.dash-to-dock click-action 'minimize'

#   --- Cayboard --- 
# Изменения раскладки CapsLock for Ctrl
$ setxkbmap -layout us -option ctrl:nocaps

# or

$ sudo vim /etc/default/keyboard

XKBOPTIONS="ctrl:swapcaps"

$ sudo dpkg-reconfigure keyboard-configuration

# or 

Чтобы постоянно изменять поведение: выполнить 

$ dconf-editor

выберите 

org.gnome.desktop.input-sources

Измените xkb-options на ['ctrl:nocaps'] (или добавьте его в любые существующие параметры)

или в командной строке ( Предупреждение - это перезаписывает существующие настройки! ):

gsettings set org.gnome.desktop.input-sources xkb-options "['ctrl:nocaps']"

# -----------------------------------------------------------------------------|
